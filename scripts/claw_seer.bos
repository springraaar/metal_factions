#define TA			// This is a TA script

#include "sfxtype.h"
#include "exptype.h"

#define SPEEDUP_FACTOR 	100

piece  base, base2, t1, t2,t1cover,t2cover, dish;

#include "tankthreads.h"


HitByWeapon(Func_Var_1, Func_Var_2)
{
	turn base to z-axis Func_Var_2 speed <105.021978>;
	turn base to x-axis Func_Var_1 speed <105.021978>;
	wait-for-turn base around z-axis;
	wait-for-turn base around x-axis;
	turn base to z-axis <0.000000> speed <30.005495>;
	turn base to x-axis <0.000000> speed <30.005495>;
	return (0);
}

Create()
{
	start-script AnimateTracks();
	return (0);
}

Activate()
{
	spin dish around y-axis speed <100.021978>;
	return (0);
}

Deactivate()
{
	spin dish around z-axis speed <0.000000>;
	return (0);
}


Killed(severity, corpsetype)
{
	if( severity <= 50 )
	{
		explode dish type SHATTER;
		return (1);
	}
	explode base type SHATTER | EXPLODE_ON_HIT;
	explode dish type FALL | SMOKE | FIRE | EXPLODE_ON_HIT;
	return (2);
}
